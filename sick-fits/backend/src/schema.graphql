# import * from './generated/prisma.graphql'

type Mutation {
	# createDog(name: String!): Dog
	createItem(
		title: String!
		description: String!
		price: Int!
		image: String
		largeImage: String
	): Item!
}
type Query {
	items: [Item]!
}
